// Send Message
function sendMessage(receiver) {}

// Receive XMTP message stream (conversation)/
function receiveMessageStream(sender, receiver) {}

// Authenticate user
function authenticate(): void {}
